/**
 * MAIN & GRAIN.
 *
 * Only the Hero is in scope for this stage. Foundations (tokens, self-hosted
 * type, motion hooks, content model, icons) are in place; the Hero components
 * themselves are not written yet.
 */
export default function App() {
  return (
    <main>
      <h1>Main &amp; Grain</h1>
      <p>Hero — work in progress.</p>
    </main>
  );
}
